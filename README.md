# Vibecraft
A vibe coded version of Minecraft I made for fun, using local LLM's.

The game is rendered in a HTML `<canvas>` element using a 3D WebGL context. Open the /src/index.html file to play.

Most of the code has been written with Qwen 3.6 27b 8bit, running locally.